import {StyleSheet} from 'react-native';
import helperSizes from '../../../themes/helperSizes';

const styles = StyleSheet.create({
  wrapperContainer: {
    flex: 1,
    justifyContent: 'center',
    paddingHorizontal: helperSizes.commonAppPadding,
  },
});
export default styles;
